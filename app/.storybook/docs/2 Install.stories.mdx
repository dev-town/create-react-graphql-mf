import { Meta } from '@storybook/addon-docs';

<Meta title="Getting started / Install" />

> ## This page should be updated for your own package!

# 1) Install (EXAMPLE)

To install simply run:

```
npm install YOUR_PACKAGE_NAME
```

# 2) Mounting (EXAMPLE)

This package contains a **X** component, this is how you mount it:

> This file should be updated to reflect this repo. Try to demonstrate how a developer would install the component in this package.

```
import { MyComponent } from 'YOUR_PACKAGE_NAME';
...
<MyComponent />
```
